<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <% if(isUser){ %>

        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/users/<%= isUser %>">Profile</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/groups">Groups</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/friends">Friends</a>
        </li>



      </ul>



      <% } else { %>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/auth/login">Log in</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/auth/register">Sign Up</a>
        </li>
      </ul>
      <% } %>

      <% if(isUser) { %>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <div class="d-flex">
            <input class="form-control me-2" type="search" name="username" placeholder="Username" id="searchText" autocomplete="off">
          </div>
          <ul id="userList" class="user-list"></ul>
        </li>
      </ul> 
   

      <input id="myId" type="hidden" value="<%= isUser %>" />

      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <li class="nav-item dropdown" style="padding: 0px 15px 0px 0px;">
          <a class="btn btn-primary dropdown-toggle" id="friendResDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Friend Requests
          </a>
          <ul id="friendReqs" class="dropdown-menu" aria-labelledby="friendResDropdown">
            <% if (friendReqs && friendReqs.length !== 0) {%>
            <%#- console.log(friendReqs) %> <%
                     friendReqs.forEach(friend=> { %>
            <li>
              <a class="dropdown-item" href="/users/<%= friend._id %>" style="margin: 0; padding: 0; text-align: center;">
                <%= friend.name %>
              </a>
            </li>
            <% }); %> <% } else { %>
            <li>
              <p style="margin: 0; padding: 0; text-align: center;">Empty</p>
            </li>
            <% } %>
          </ul>
        </li>
      </ul>
      <% }%>

      <% if(isUser) { %>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <form id="logout-form" method="POST" action="/auth/logout">
            <button type="submit" id="logout" class="btn btn-danger">Logout</button>
          </form>
        </li>
      </ul>
      <%}%>
    </div>
  </div>
</nav>





